{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Deploy",
      "cwd": "${workspaceFolder}",
      "command": "GitPush.bat"
    }
  ]
}
